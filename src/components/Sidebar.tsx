"use client";

/**
 * Browse category sidebar — thin wrapper around the taxonomy tree so
 * merchandising layouts can import `Sidebar` as specified in the product brief.
 */
export { CategoryTree as Sidebar } from "@/components/browse/CategoryTree";
export { CategoryTree } from "@/components/browse/CategoryTree";
