"use client";

/**
 * Melt calculator lives inside the combined market tools widget.
 * Re-exported here so existing imports keep working.
 */
export { MarketToolsWidget, MarketToolsWidget as MeltCalculator } from "@/components/MarketToolsWidget";
